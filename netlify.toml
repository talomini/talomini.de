[context.production]
  command = "hugo --gc --minify"

[context.production.environment]
  HUGO_VERSION = "0.84.0"
  HUGO_ENV = "production"

[context.preview]
  command = "hugo --gc --buildDrafts --config config.toml,config.preview.toml --baseURL $DEPLOY_PRIME_URL/"

